using UnityEngine;

public class GetAwayIdentifier : MonoBehaviour
{
    // This script is pointless (kinda)
    // Its used to find the get away point by FindAnyObjectByType<GetAwayIdentifier>().gameObject
    // Couldn't really find a better way :(
}
